apiVersion: v2
name: crusoe-watch-agent
description: Helm chart for cwa-manager (crusoe-watch-agent v2)
type: application
# Stamped by the release pipeline at render time.
version: "@@CHART_VERSION@@"
appVersion: "@@CHART_APP_VERSION@@"
