{{- if .Values.cme.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: crusoe-metrics-exporter
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: crusoe-metrics-exporter
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: crusoe-metrics-exporter
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: crusoe-metrics-exporter
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: crusoe-metrics-exporter
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: crusoe-metrics-exporter
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      hostPID: true
      serviceAccountName: crusoe-metrics-exporter
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      tolerations:
        {{- toYaml .Values.tolerations | nindent 8 }}
      containers:
        - name: crusoe-metrics-exporter
          image: "{{ .Values.cme.image }}:{{ .Values.cme.tag }}"
          ports:
            - name: metrics
              containerPort: {{ .Values.cme.port }}
              protocol: TCP
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: PORT
              value: {{ .Values.cme.port | quote }}
            - name: DISKSTATS_PATH
              value: "/host/proc/diskstats"
            - name: MOUNTSTATS_PATH
              value: "/host/proc/self/mountstats"
          livenessProbe:
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: proc
              mountPath: /host/proc
              readOnly: true
          resources:
            {{- toYaml .Values.resources.cme | nindent 12 }}
          securityContext:
            privileged: true
      volumes:
        - name: proc
          hostPath:
            path: /proc
{{- end }}
